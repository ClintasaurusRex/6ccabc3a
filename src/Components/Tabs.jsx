// import { useState } from "react";

const Tabs = () => {
  // const [active, setActive] = useState(false);

  return (
    <div>
      <header>
        <button>Inbox</button>
        <button>All Calls</button>
      </header>
    </div>
  );
};
export default Tabs;
